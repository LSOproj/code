services:
  videorent:
    build: .
    image: videorent
    container_name: videorent-client-server
    stdin_open: true
    tty: true
    volumes:
      - database_volume:/app/persist

volumes:
  database_volume:
